name: Build Electron PDV

on:
  push:
    branches: [main]
    paths:
      - 'electron/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: electron

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: electron/package-lock.json

      - name: Instalar dependencias
        run: npm ci

      - name: Build PDV Windows (instalador NSIS)
        run: npm run build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload instalador NSIS
        uses: actions/upload-artifact@v4
        with:
          name: MercadinhoPDV-setup
          path: electron/dist/MercadinhoPDV-setup.exe
          retention-days: 30
